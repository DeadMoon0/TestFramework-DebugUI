# TestFramework-DebugUI
A UI for Debugging a TestFramework Timeline.

The DebugUI data layer now exposes a canonical state tree of `Run -> Stage -> Layer -> Step -> Attempt` plus query helpers for ordered traversal, latest-attempt summaries, and log-focused views.
