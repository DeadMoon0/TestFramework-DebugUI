<UserControl x:Class="TestFramework.DebugUI.Controls.Detail.UC_ArtifactItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Border CornerRadius="2.5" Background="{StaticResource SurfaceRaised}" Margin="0 0 0 4" Padding="8 6">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Viewbox Width="14" Height="14" Margin="0 1 8 0" VerticalAlignment="Top">
                <Canvas Width="16" Height="16">
                    <Path x:Name="pIcon" StrokeThickness="1.2" Stroke="#FF7A7A7A"/>
                </Canvas>
            </Viewbox>

            <TextBlock x:Name="tbKey" Grid.Column="1" Style="{StaticResource BodyText}"
                       TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>

            <TextBlock x:Name="tbState" Grid.Row="1" Grid.Column="1" Style="{StaticResource MutedText}" FontSize="10"/>

            <!--
                The journey, one chip per captured version. This is the surface the artifact version
                work in Core was for: before it, only the first version ever reached a consumer.
            -->
            <WrapPanel x:Name="wpVersions" Grid.Row="2" Grid.Column="1" Margin="0 4 0 0"/>
        </Grid>
    </Border>
</UserControl>
