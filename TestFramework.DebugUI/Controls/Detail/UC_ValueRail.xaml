<UserControl x:Class="TestFramework.DebugUI.Controls.Detail.UC_ValueRail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Border Style="{StaticResource PanelOverlay}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Style="{StaticResource PanelHeading}" Text="VALUES"/>

                <!-- How many of each, so the heading says what the run actually produced. -->
                <TextBlock x:Name="tbCounts" Grid.Column="1" Style="{StaticResource MutedText}" FontSize="9"
                           VerticalAlignment="Center" Margin="0 0 12 0"/>
            </Grid>

            <!--
                What this run is being compared against. Present whether or not a comparison could be
                made: "no earlier passing run" is an answer, and leaving the line out would make the
                absence of badges look like "nothing changed".
            -->
            <TextBlock x:Name="tbBaseline" Grid.Row="1" Margin="14 0 14 6" FontSize="9"
                       TextWrapping="Wrap" Foreground="{StaticResource TextFaint}"
                       Visibility="Collapsed"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="spValues" Margin="5 0 5 5"/>
            </ScrollViewer>

            <TextBlock x:Name="tbEmpty" Grid.Row="2" Style="{StaticResource MutedText}" Margin="14 4 14 0"
                       Text="This run produced no values."/>
        </Grid>
    </Border>
</UserControl>
