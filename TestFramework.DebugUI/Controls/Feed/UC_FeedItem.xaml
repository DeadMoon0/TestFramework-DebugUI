<UserControl x:Class="TestFramework.DebugUI.Controls.Feed.UC_FeedItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Border x:Name="bRoot" CornerRadius="4" Background="{StaticResource SurfaceRaised}" Margin="0 0 0 3"
            Padding="8 5" MouseLeftButtonUp="Root_MouseLeftButtonUp">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border x:Name="bSeverity" Width="4" Height="4" CornerRadius="4" VerticalAlignment="Center"
                    Margin="0 0 8 0" Background="{StaticResource TextFaint}"/>

            <TextBlock x:Name="tbWhen" Grid.Column="1" Style="{StaticResource MutedText}" FontSize="10"
                       Foreground="{StaticResource TextFaint}" Margin="0 0 8 0" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="2">
                <TextBlock x:Name="tbTitle" Style="{StaticResource BodyText}" FontSize="11"/>
                <TextBlock x:Name="tbDetail" Style="{StaticResource MutedText}" FontSize="10" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
