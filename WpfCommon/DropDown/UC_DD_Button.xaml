<UserControl x:Name="userControl" x:Class="WPFCommon.DropDown.UC_DD_Button"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFCommon.DropDown"
             mc:Ignorable="d" Height="25.96">
    <Border x:Name="border" CornerRadius="5" Cursor="Hand" MouseLeftButtonUp="border_MouseLeftButtonUp">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="#FF232323"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Trigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                                     From="#FF232323" To="#FF333333"
                                                     Duration="0:0:0.2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>
                        <Trigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                                     From="#FF333333" To="#FF232323"
                                                     Duration="0:0:0.2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.ExitActions>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="cdIcon" Width="0"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Viewbox x:Name="vbImage" Margin="5">
                <Image x:Name="iIcon" Source="{Binding Path=Icon,ElementName=userControl}"/>
            </Viewbox>
            <Viewbox x:Name="vbIcon" Margin="5" Visibility="Collapsed"/>
            <Label x:Name="lText" Grid.Column="1" VerticalAlignment="Center" Foreground="White" Content="{Binding Path=Text,ElementName=userControl}"></Label>
        </Grid>
    </Border>
</UserControl>
